// Copy this file to Secrets.xcconfig (same directory) and paste in your real key.
// Secrets.xcconfig is gitignored — this .example file is the only one tracked by git.
ANTHROPIC_API_KEY = your-key-here
